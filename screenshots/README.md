Add screenshots here before submitting to GitHub.

Required screenshots:
- login.png        → Login page
- register.png     → Register / Create Account page  
- dashboard.png    → Main task dashboard
- task-modal.png   → Create / Edit task modal
- swagger.png      → Swagger UI at http://localhost:8080/swagger-ui.html
- database.png     → MySQL database tables (optional)
